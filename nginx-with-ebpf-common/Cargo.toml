[package]
name = "nginx-with-ebpf-common"
version = "0.1.0"
edition = "2021"

[features]
default = []
user = ["aya"]

[dependencies]
aya = { version = "0.12", optional = true }
aya-ebpf-cty = "0.2.1"

[lib]
path = "src/lib.rs"
